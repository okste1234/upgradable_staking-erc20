// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

library LibStake {
    // // custom errors
    // error ADDRESS_ZERO();
    // error INVALID_AMOUNT();
    // error INSUFFICIENT_AMOUNT();
    // error USER_HAS_NO_STAKE();
    // error NO_REWARD_TO_CLIAM();
    // // events
    // event stakingSuccessful(address _staker, uint256 _amount);
    // event claimSuccessful(address _staker, uint256 _amount);
    // event unStakeSuccessful(address _staker, uint256 _amount);
    // // dynamic storage location
    // // bytes32 constant STAKE_STORAGE_POSITION = keccak256("diamond.standard.stake.storage");
    // // stake info storage
    // struct StakeInfo {
    //     uint256 amountStaked;
    //     uint256 timeStaked;
    //     uint256 reward;
    // }
    // // stake storage
    // struct StakeStorage {
    //     address erc20Token;
    //     address rewardToken;
    //     uint8 rewardRate;
    //     mapping(address => StakeInfo) stakes;
    // }
    // // assigning slot position to stakeStorage
    // // function stakeStorage() internal pure returns (StakeStorage storage ss) {
    // //     bytes32 position = STAKE_STORAGE_POSITION;
    // //     assembly {
    // //         ss.slot := position
    // //     }
    // // }
}
